<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.faraway.domain.post.repository">

    <select id="findAllByCondition"  parameterType="PostSearchCondition" resultType="PostListResponseDto">
        select p.id, p.member_id, m.member_name, p.category, p.title, p.hit, p.modified_date
        from post p join member m
        on p.member_id = m.id
        <if test="!key.equals('none')">
            and ${key} like concat('%', #{word}, '%')
        </if>
        <if test="!orderBy.equals('none')">
            order by ${orderBy} ${orderByDir}
        </if>
        <if test="orderBy.equals('none')">
            order by p.modified_date ${orderByDir}
        </if>
    </select>

</mapper>